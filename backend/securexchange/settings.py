# Django settings file configured for SecureXchange to support POST and CORS

import os

# Database Configuration
FOREIGN_Key = {
    email http:
        share drive. directlib.docker clients. orig update modules test confis.   
 